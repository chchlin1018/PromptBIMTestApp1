{
  "mcpServers": {
    "promptbim": {
      "command": "python",
      "args": ["-m", "promptbim.mcp.server"],
      "env": {
        "PYTHONPATH": "src",
        "PROMPTBIM_MCP_TIMEOUT": "120"
      }
    }
  }
}
